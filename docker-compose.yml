services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: scheduling
      POSTGRES_PASSWORD: scheduling
      POSTGRES_DB: scheduling
    ports:
      - "5439:5432"
    volumes:
      - scheduling-pgdata:/var/lib/postgresql/data

volumes:
  scheduling-pgdata:
